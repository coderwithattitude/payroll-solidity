dist: trusty
sudo: required
group: beta
language: node_js
node_js:
  - "8"
cache:
  directories:
    - node_modules
install: npm install
script:
  - npm run test
  - npm run build
deploy:
  provider: pages
  skip-cleanup: true
  keep-history: true
  local-dir: "dist"
  on:
    branch: deploy/travis